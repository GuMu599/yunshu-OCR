# pdf2md — 零 token 离线 PDF→Markdown 转换依赖
# 与 litwise-ocr 基础依赖重叠; 版面/公式/OCR 全部本地离线

# PDF 处理
PyMuPDF>=1.23.0
numpy>=2.0.0
Pillow>=12.3.0,<13

# 版面检测（权重由 python -m pdf2md.models install 安装）
doclayout_yolo>=0.0.2b1

# RapidOCR (onnxruntime CPU, vendored 适配器在 models/production/rapidocr-adapter)
onnxruntime>=1.17.0
opencv-python>=4.8.0
omegaconf>=2.3.0
pyclipper>=1.3.0
shapely>=2.0.0
colorlog>=6.7.0
requests>=2.28.0
tqdm>=4.66.0
psutil>=5.9.0

# 公式识别模型（图像→LaTeX）；转换时只读取经清单校验的本地权重
pix2tex>=0.1.4
